DB_HOST=localhost
DB_USER=root
# IMPORTANT: Replace the dummy password below with your actual root password
DB_PASSWORD=Password
DB_NAME=comlab_monitoring
PORT=5000
# Fallback admin account (used when admin_credentials table is empty)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=admin123
